import java.util.*;

class Solution {

    public int[] loudAndRich(int[][] richer, int[] quiet)
    {

        int n = quiet.length;

        List<Integer>[] graph = new ArrayList[n];

        for (int i = 0; i < n; i++) 
        {
            graph[i] = new ArrayList<>();
        }
        for (int[] r : richer) 
        {
            int a = r[0];
            int b = r[1];

            graph[b].add(a);
        }

        int[] ans = new int[n];
        Arrays.fill(ans, -1);

        for (int i = 0; i < n; i++) 
        {
            dfs(i, graph, quiet, ans);
        }

        return ans;
    }

    private void dfs(int person,
                     List<Integer>[] graph,
                     int[] quiet,
                     int[] ans) {

        if (ans[person] != -1)
        {
            return;
        }

        ans[person] = person;

        for (int richerPerson : graph[person]) 
        {

            dfs(richerPerson, graph, quiet, ans);

            if (quiet[ans[richerPerson]] < quiet[ans[person]])
            {
                ans[person] = ans[richerPerson];
            }
        }
    }
}
